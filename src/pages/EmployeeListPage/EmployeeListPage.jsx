import styles from './EmployeeListPage.module.css';

const EmployeeListPage = ({}) => {
	return (
		<div className={styles.employeelistpage}>
 			EmployeeListPage works!
 		</div>
	);
};

export default EmployeeListPage;